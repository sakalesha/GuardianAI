{
  "name": "guardianai-root",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "lint": "npm run lint -w frontend && npm run lint -w backend",
    "preview": "npm run preview -w frontend",
    "backend:dev": "npm run dev -w backend",
    "run": "npx concurrently -c \"blue,green,magenta\" -n \"ui,api,ml\" \"npm run dev -w frontend\" \"npm run dev -w backend\" \"cd ml && python ml_api.py\""
  }
}
